# Test-only dependencies. Installed in CI for the unit-tests job ONLY — these
# are deliberately NOT in the Dockerfile / runtime image, so the image SBOM and
# CVE posture stay unchanged.
pytest==8.3.3
httpx==0.27.2
